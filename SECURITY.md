# Security and experimental status

This is experimental kernel and DMA code. Known incomplete validation includes foreign-QPN doorbells, process exit during outstanding direct work and device removal with mapped pages. The project does not claim production hardening or support for unlisted macOS builds.

Do not post credentials, memory dumps, kernel collections, private logs or working remote-access details in public issues. A private reporting channel must be configured on the hosting repository before public release.
